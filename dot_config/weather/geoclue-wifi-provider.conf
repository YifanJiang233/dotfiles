# GeoClue's distribution-supported Positon endpoint. This fallback is used only
# when the default BeaconDB provider has no coverage for nearby access points.
[wifi]
enable=true
url=@POSITON_URL@
submit-data=false
